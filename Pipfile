[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
server = "python run.py"
lint = "pylint --rcfile=.pylintrc src/"
lint-report = "pylint --rcfile=.pylintrc --report=y src/"
test = "pytest"
test-list = "pytest -s -v"
test-list-full = "pytest -s -vv"

[packages]
pylint = "*"
pre-commit = "*"
sqlalchemy = "*"
pytest = "*"
mock-alchemy = "*"
pytest-mock = "*"
flask = "*"
flask-cors = "*"
pydantic = "*"

[dev-packages]

[requires]
python_version = "3.12"
python_full_version = "3.12.3"
